import { StyleSheet } from 'react-native';

export default StyleSheet.create( {
  measureContainer: {
    width: 0,
    height: 0,
    overflow: 'hidden',
  },
  measureText: {
    position: 'absolute',
    top: 0,
    left: 0,
  },
} );
